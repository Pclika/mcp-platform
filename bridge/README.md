# Bridge

This directory contains the MCP-facing bridge layer for the Pclika platform.

Its role is to expose stable tool contracts for AI coding tools while keeping the firmware and hardware layers reusable.

