# Pclika MCP config for Codex CLI
# Docs: https://pclika.com/docs/clients/codex
#
# STDIO mode (default): Codex spawns pclika-bridge as a subprocess.
# SSE  mode (adaptive): Run 'pclika-bridge --serve' once, then use url variant below.
# Auto-install: run 'pclika-setup' after 'pip install pclika-bridge'

# ── STDIO mode (default) ───────────────────────