{
  "_mode": "stdio",
  "_comment": "Switch to SSE adaptive mode: set _mode to 'sse' and run 'pclika-bridge --serve' once. Or run: pclika-setup --sse",

  "mcpServers": {
    "pclikaPlatform": {
      "command": "pclika-bridge",
      "args": ["--port", "auto", "--baud", "115200"],
      "env": {},
      "description": "Pclika MCP Bridge — ESP32-S3 hardware via USB-Serial"
    },
    "pclikaHDL": {
      "command": "pclika-hdl-bridge",
      "args": ["--device", "ice40up5k", "--package", "sg48", "--freq", "12"],
      "env": {},
      "description": "Pclika HDL Bridg