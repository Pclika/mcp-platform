{
  "$schema": "https://opencode.ai/config.json",

  // Pclika MCP config for OpenCode
  // Auto-install: pclika-setup  (after pip install pclika-bridge)
  // SSE mode:     pclika-setup --sse  (run pcl