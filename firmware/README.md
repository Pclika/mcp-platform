# Firmware

This directory holds the reusable device runtime for the Pclika platform.

Initial targets:

- `esp-idf/`
- `arduino/`

The firmware layer should expose stable capability domains that can be mapped into MCP tools.

