# Scripts

This directory is reserved for helper scripts that support:

- build
- flash
- monitor
- environment validation
- project scaffolding

The scripts should stay simple enough to be discoverable by both humans and AI tools.

